---
import { getCollection } from 'astro:content';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { SERIES, SITE_DESCRIPTION, SITE_TITLE } from '../consts';

const posts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);
const sections = SERIES.map((series) => ({
	...series,
	posts: posts.filter((p) => p.data.series === series.id),
}));
const currentSections = sections.filter((s) => !s.archived);
const archivedSections = sections.filter((s) => s.archived);
const unsorted = posts.filter((p) => !SERIES.some((s) => s.id === p.data.series));
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />
		<main class="home">
			<section class="intro">
				<p>Silly mental adventures in the search for novelty&hellip;</p>
			</section>
			<section class="image-band">
				<img src="/images/home.webp" alt="" />
			</section>
			{
				currentSections.map((series) => (
					<section class="lane">
						<div class="series-heading">
							<h4>
								<a href={`/${series.id}/`}>{series.title}</a>
							</h4>
							{series.description && <p class="series-description">{series.description}</p>}
						</div>
						<ul class="post-list">
							{series.posts.map((post) => (
								<li>
									<a href={`/blog/${post.id}/`}>{post.data.title}</a>
									<span class="date">
										{post.data.pubDate.toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric', timeZone: 'UTC' })}
									</span>
								</li>
							))}
						</ul>
					</section>
				))
			}
			{
				unsorted.length > 0 && (
					<section class="lane">
						<h4>Other Writing</h4>
						<ul class="post-list">
							{unsorted.map((post) => (
								<li>
									<a href={`/blog/${post.id}/`}>{post.data.title}</a>
									<span class="date">
										{post.data.pubDate.toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric', timeZone: 'UTC' })}
									</span>
								</li>
							))}
						</ul>
					</section>
				)
			}
			{
				archivedSections.length > 0 && (
					<section class="lane archived">
						<div class="series-heading">
							<h4>Archived Projects</h4>
							<p class="series-description">Earlier work, kept for the record.</p>
						</div>
						<div class="archived-series">
							{archivedSections.map((series) => (
								<div class="archived-item">
									<h5>
										<a href={`/${series.id}/`}>{series.title}</a>
									</h5>
									<ul class="post-list">
										{series.posts.map((post) => (
											<li>
												<a href={`/blog/${post.id}/`}>{post.data.title}</a>
												<span class="date">
													{post.data.pubDate.toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric', timeZone: 'UTC' })}
												</span>
											</li>
										))}
									</ul>
								</div>
							))}
						</div>
					</section>
				)
			}
		</main>
		<Footer />
	</body>
</html>

<style>
	main.home {
		width: 100%;
		max-width: 100%;
		margin: 0;
		padding: 0;
	}
	.intro {
		padding: 6.5em 2.5em;
	}
	.intro p {
		max-width: 48em;
		margin: 0;
	}
	.image-band {
		width: 100%;
		height: 46vw;
		min-height: 320px;
		max-height: 560px;
	}
	.image-band img {
		width: 100%;
		height: 100%;
		object-fit: cover;
		object-position: 50% 50%;
		display: block;
		border-radius: 0;
	}
	.lane {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 2em;
		padding: 4em 2.5em;
	}
	.lane h4 {
		margin: 0;
	}
	.lane h4 a {
		color: var(--navy);
		text-decoration: none;
	}
	.lane h4 a:hover {
		text-decoration: underline;
		opacity: 1;
	}
	.series-description {
		margin: 0.6em 0 0 0;
		max-width: 24em;
	}
	.post-list {
		list-style: none;
		margin: 0;
		padding: 0;
	}
	.post-list li {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 0.75em;
		align-items: baseline;
		margin-bottom: 1.6em;
	}
	.post-list a {
		font-family: var(--font-syne), 'Syne', sans-serif;
		font-size: 1.45rem;
		letter-spacing: -0.02em;
		color: var(--navy);
		text-decoration: none;
		line-height: 1.17;
	}
	.post-list a:hover {
		text-decoration: underline;
		opacity: 1;
	}
	.post-list .date {
		font-size: 1rem;
	}
	.lane.archived {
		border-top: 1px solid rgba(37, 53, 81, 0.18);
		padding-top: 3em;
	}
	.lane.archived h4 {
		color: var(--muted);
	}
	.archived-item h5 {
		margin: 0 0 1.2em 0;
		font-size: 1rem;
		font-weight: 600;
		letter-spacing: 0.04em;
		text-transform: uppercase;
		color: var(--muted);
	}
	.archived-item h5 a {
		color: var(--muted);
		text-decoration: none;
	}
	.archived-item h5 a:hover {
		text-decoration: underline;
	}
	.lane.archived .post-list a {
		font-size: 1.15rem;
		color: var(--muted);
	}
	@media (max-width: 720px) {
		.intro {
			padding: 4.5em 1.5em;
		}
		.lane {
			grid-template-columns: 1fr;
			padding: 3em 1.5em;
		}
	}
</style>
